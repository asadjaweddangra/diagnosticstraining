{
  "version": 2,
  "builds": [
    {
      "src": "app/next.config.mjs",
      "use": "@vercel/next"
    }
  ],
  "installCommand": "cd app && npm install",
  "buildCommand": "cd app && npm run build",
  "routes": [
    { "src": "/(.*)", "dest": "app/$1" }
  ]
}

