{
  "version": 2,
  "builds": [
    {
      "src": "app/package.json",
      "use": "@vercel/next"
    }
  ],
  "installCommand": "cd app && npm install",
  "buildCommand": "cd app && npm run build",
  "routes": [
    { "src": "/(.*)", "dest": "app/$1" }
  ]
}

